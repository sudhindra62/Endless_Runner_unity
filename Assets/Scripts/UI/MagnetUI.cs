using UnityEngine;

public class MagnetUI : MonoBehaviour
{
    public void UpdateMagnetCount(int count)
    {
        // This is a placeholder to resolve a compile error.
        // The actual UI update logic should be in a separate UI-specific script.
    }
}
